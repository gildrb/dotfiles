# Neovim Configuration Development

- Read the repository before changing it.
- Keep the Death Note palette aligned with `gildrb/pi-configuration`.
- Prefer small, readable Lua modules over framework-like abstractions.
- Keep `lazy-lock.json` committed so installs are reproducible.
- Run `./scripts/check.sh` after changes.
